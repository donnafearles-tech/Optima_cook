# Settings for Backend (on Cloud Run).
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
  - variable: GCLOUD_PROJECT
    value: "studio-99491860-5533f"
    availability:
      - BUILD
      - RUNTIME

  #- variable: GOOGLE_APPLICATION_CREDENTIALS
    secret: cocinaoptimavertex
    availability:
      - BUILD
      - RUNTIME
